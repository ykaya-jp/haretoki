{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "command": "npx prettier --write $CLAUDE_FILE_PATH 2>/dev/null || true",
        "description": "ファイル編集後に自動フォーマット"
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Write",
        "command": "echo $CLAUDE_FILE_PATH | grep -qE '\\.(env|key|pem)$' && echo 'BLOCK: Sensitive file' && exit 1 || true",
        "description": "機密ファイルへの書き込みをブロック"
      }
    ]
  }
}
